use std::*;fn main(){let mut v=Vec::new();for a in env::args().skip(1){let b=a.parse().unwrap();v.push(thread::scoped(move||{old_io::timer::sleep(time::duration::Duration::seconds(b));println!("{}", b)}))}for t in v{t.join()}}